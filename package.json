{
  "name": "statistical-analysis-probability-tools",
  "version": "1.0.0",
  "description": "Web application for statistical analysis, hypothesis testing, and probability distributions",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["statistics", "probability", "hypothesis-testing", "regression", "data-visualization"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
